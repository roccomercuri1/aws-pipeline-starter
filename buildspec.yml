version: 0.2

env:
  secrets-manager:
    MY_SECRET: "emile:test"

phases:
  build:
    commands:
      - echo "Hello World from BuildSpec.yml"
      - echo "$name"

artifacts:
  files:
    - Dockerfile